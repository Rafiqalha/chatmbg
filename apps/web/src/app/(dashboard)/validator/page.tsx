import { MenuValidator } from '@/components/validator/menu-validator';

export const metadata = {
  title: 'Validator Menu Gizi',
};

export default function ValidatorPage() {
  return <MenuValidator />;
}
